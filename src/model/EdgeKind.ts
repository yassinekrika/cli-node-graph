/** All supported edge kinds in the knowledge graph. */
export enum EdgeKind {
  IMPORTS = 'IMPORTS',
  EXPORTS = 'EXPORTS',
  CALLS = 'CALLS',
  REFERENCES = 'REFERENCES',
  EXTENDS = 'EXTENDS',
  IMPLEMENTS = 'IMPLEMENTS',
  CREATES = 'CREATES',
  USES = 'USES',
  RETURNS = 'RETURNS',
  ACCEPTS = 'ACCEPTS',
  DECLARES = 'DECLARES',
  BELONGS_TO = 'BELONGS_TO',
  CONTAINS = 'CONTAINS',
  DEPENDS_ON = 'DEPENDS_ON',
  INJECTS = 'INJECTS',
  DECORATED_BY = 'DECORATED_BY',
  DYNAMIC_IMPORT = 'DYNAMIC_IMPORT',
  TYPE_REFERENCE = 'TYPE_REFERENCE',
}
